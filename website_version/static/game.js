// const socket = io();

// socket.on('connect', () => {
//     socket.emit('join', {});
// });

// // Add handler for game start
// socket.on('start', data => {
//     document.getElementById('score').innerText = `Opponent: ${data.opponent}`;
// });

// function makeMove(move) {
//     socket.emit('move', { move: move });
// }